# Changelog

本项目的所有值得注意的改动都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 0.6.0 起开始发布 Release 包：打 `v*` tag 触发 CI 构建签名的 DMG。
> 此前的 0.1.0–0.5.1 属于设计调试阶段，只有变更记录，没有发布产物。

## [Unreleased]

## [1.2.0] - 2026-08-27

### Added

- **PR 状态变了会弹一条系统通知。** 自动刷新发现某个 PR 从一个状态变成另一个状态时通知你 ——
  「#77400 等 review → 可合并」这种。点通知直接跳到那个 PR。

  几条刻意的取舍：

  - **只有自动刷新发现的变化才通知。** 你自己点刷新时变化就在眼前，再弹一遍是噪音
  - **刚启动的第一次拉取算基线**，不补报关机期间的变化 —— 一开机炸一屏比不通知更烦。
    快照只放内存，不落盘
  - **任何一头是「未知」都不报。** 拉取失败会把状态打成未知，一来一回就是两条假通知
  - 一次多个变化就**汇总成一条**（列前 4 个），不刷屏
  - 评审视角有**新待办**时也通知一条 —— 「有人开始等我」正是最该被叫醒的时刻

  设置 → 刷新里可以关掉，旁边有「发一条试试」按钮和跳转系统通知设置的链接。

### Changed

- **`build.sh` 现在会用稳定签名身份签包**，身份从 `bootstrap_local_signing.sh` 写的
  `signing.env` 里读 —— 跟 `package_app.sh` 走同一条路，两个脚本对「用哪个身份」的判断
  必须一致，不然本地跑出来的包和打包出来的包不是一个身份。签不上时退回 ad-hoc，
  并说清楚是「还没配身份」还是「配了但当前 shell 签不了」。

  这不是可有可无的：**macOS 不给 ad-hoc 签名的 app 发通知权限**。ad-hoc 签名每次构建都变，
  系统没法把授权记在一个稳定身份上。实测同一份代码、同样装在 `/Applications`：

  ```
  ad-hoc 签名        -> granted=false, UNErrorDomain error 1, alertStyle=0
  PrWaiter Signing   -> granted=true,  alertStyle=1（横幅）
  ```

  而且失败时 app 根本不会出现在「系统设置 → 通知」列表里，看起来就像功能没做。
  要在本地调通知，先跑一次 `scripts/bootstrap_local_signing.sh`。

### Fixed

- `build.sh` 里 `"「$name」"` 这种写法会被 bash 误当成变量名的一部分（多字节字符的头一个字节）
  —— 跟之前 `${TAG}，` 那次是同一类坑，改成 `${name}`。全仓库扫过，没有别的同类写法

## [1.1.1] - 2026-08-22

### Fixed

- **「等维护者批准 workflow」被显示成「CI 运行中」。** 现在是单独一档 **「CI 等批准」**（橙，✋）。

  fork PR 的 CI 要维护者点一下「批准运行」才会跑。这种状态下 check suite 长这样：

  ```
  status = COMPLETED        conclusion = ACTION_REQUIRED       0 条 check run
  ```

  原来只读了 `status`，看到 COMPLETED 就当成「跑完了」，于是既没有 check 也没有必过项，
  只好靠「必过项没报上来」那条规则兜成「运行中」—— 说法不准：**不点批准的话一步都不会跑，
  它不是在跑，是在等人**。现在连 `conclusion` 一起读，认出这一档。

  相应地把 `ACTION_REQUIRED` 从「失败」集合里挪出来了 —— 它不是失败，是等人。

  实测 `apache/maka` 的 #3448 / #3443 / #3435 / #3430 就是这个状态，GitHub 页面上写的是
  `1 workflow awaiting approval`。

## [1.1.0] - 2026-08-22

多了一个 **reviewer 视角**。此前整个 App 都是创作者视角 —— 我写的 PR 排到哪了；
这一版补上另一半：**别人卡在我这里的事**。

动机很实际：GitHub 的邮件多到根本看不完，别人的流程卡在你这里，往往就是因为那封邮件被埋了。

### Added

- **刷新和设置中间多了一个「切换到评审视角」按钮**，右上角带待办数气泡（有「等我批准」时变橙）。
  切过去是一份「谁在等我」的清单，按「卡得多死」排序：

  1. **等我批准** —— workflow / 部署卡着等我点批准，不批就永远不跑，作者在那边干等
  2. **请我 review** —— 指名或按团队请我看的
  3. **指派给我** —— 别人建的 PR 把我设成了负责人
  4. **@ 到我** —— 有人在 PR 里点了我，等我回话

  每行显示：仓库、编号、标题、目标分支、在等我的那个人、CI 状态、**别人等了我多久**。
  标签上带未处理数量，有「等我批准」时变橙色。

- 这一页刻意跟看板长得不一样：**没有树、没有拖拽、不能编辑**。别人的 PR 我管不着合并顺序，
  只需要一份从上往下办完的清单。

### 实现要点

- **只列别人的 PR**（搜索里带 `-author:@me`）。我自己的 PR 归看板那边管，混进来只是噪音。
  同理，我已经 review 完批准了的也不在这儿 —— 球在作者那边，我不是瓶颈了
- **「等我批准」得单独查，不能只看 review 清单**：随便一个贡献者的 fork PR 卡着等我批，
  没人会请我 review，搜索里根本看不到它
- 两种批准形态都认：环境审批（run 处于 `waiting`，靠 `pending_deployments` 的
  `current_user_can_approve` 确定是不是等**我**）和 fork 首次贡献者
  （`status=action_required` 且 `conclusion` 为 null —— 光按 `status=action_required` 过滤会捞到
  一大堆「跑完了、结论是 action_required」的历史 run，实测某仓库 1148 个）
- **只查我有写权限的仓库**：批 workflow 要写权限，只读仓库连查都不用查，省一次 5 秒的请求
- **「别人从什么时候开始等我」取自 timeline**（`REVIEW_REQUESTED_EVENT` / `ASSIGNED_EVENT`），
  不是 PR 的更新时间 —— 三天前请我、刚被作者推了一版，不该排到「十分钟前请我」前面
- 仓库名显示**响应里返回的真名**，不是配置里填的那个：仓库改名 / 转移后 GitHub 会重定向，
  用旧名照样查得到，但显示旧名会让人对不上号（实测配置里的 `maka-agent/maka-agent`
  现在已经是 `apache/maka`）
- **「@ 到我」怎么消掉**：另两档 GitHub 自己会撤（提交 review 后请求自动撤销、批准后 run 就走了），
  只有 @ 是历史事实，PR 开着就永远匹配 `mentions:@me`，不处理会越堆越多。规则是
  **「有人在我最后一次发言之后又点了我」才算还欠着** —— 提交 review（含批准）、新加评论、
  回复他，任何一种都算我接手了，**不必回复那条 @ 本身**；我说完之后别人再点我，
  它会带着新时间戳重新冒出来。行内评论和 PR 正文里点我都算，我自己 @ 自己不算。
  扫不到明确的「@我」时（按团队 @ 的那种）用更新时间兜底 —— 宁可显示出来让人自己判断，
  也别悄悄吞掉
- **「忽略本次」按钮**：有些 @ 就是知会一声、不需要我回话，点一下消掉。按**这一次被点的时间**记账，
  下次有人再 @ 我（时间更晚）还会重新冒出来 —— 不是永久屏蔽这个 PR。
  只有 @ 那一档有这个按钮，另两档 GitHub 自己会撤
- **别人先把 PR 合掉之后，「等我批准」也要消失**。另三档搜索里带 `is:open`，PR 一合上自动就不在了；
  但批准项是从 workflow run 来的，**PR 合了那个 run 还挂在 `waiting` 上**，不管它就会一直显示 ——
  而流程上早就走完了，没人被我卡着。所以批准项会再查一次对应 PR 的状态，已合并 / 已关闭的直接扔掉
- **切过去不再闪一下「还没登录 gh」。** 启动时探测 gh 和拉清单是并发跑的，账号还没探测出来
  就去查，会先报个假错再自己好。现在探测这件事全局只做一次、谁要账号谁等它 ——
  看板那边的自动导入也一样（原来账号没探测出来时会悄悄跳过导入）
- 拉取中显示「正在看谁在等你…」，不再拿空清单和红色报错糊弄 —— 一次请求 5 秒起，
  空着不动会让人以为坏了
- 切换按钮的文案跟着视角变：贡献视角上是「切换到评审视角」+ 待办数气泡；
  评审视角上是「切换回贡献视角」，不带气泡 —— 人都在清单里了，数字就在眼前
- 评审视角里不再显示项目标签：那份清单是**跨全部仓库**的，摆着标签会让人以为被它过滤了。
  改成显示「谁在等我 · N 个仓库」
- **一次刷新把所有项目和评审清单全刷了**，不再只刷当前那个。各项目并发拉
  （慢在握手，并发起来总耗时约等于最慢的那一个）。不这么做的话，切到别的项目总要再等一次，
  而且切换按钮上的待办数会一直是旧的 —— 那个数字要有用，就得一直是准的
- **两个视角是两套东西，互不串门**：看板已经在跟的 PR，评审清单里不再出现。
  主要挡的是机器人代建的 backport —— 作者是 bot、把我设成 assignee，看板按 assignee
  把它导进来当我的贡献（本来就是我的改动被 backport），但评审清单按 `assignee:@me`
  也会搜到它。对账用**查询时的仓库名**，不是 GitHub 返回的真名 —— 仓库改过名的话两者不一样

### Fixed

- **拉取失败按项目分开记**。所有项目并发刷新之后，别的项目挂了会显示在你正看着的这个项目头上
- **GraphQL 抽风时重试一次**。GitHub 偶尔回 5xx，这条网络本身也不稳；并发刷新之后更容易撞上，
  一个项目挂了整块就空了。重试的代价只在真失败时才付

### 其它

- 清单只查已配置的那几个项目仓库，跟看板分开缓存。启动时后台拉一次
  （标签上的数字得一打开就准），之后跟着自动刷新走 —— 但**只有你打开过这一页才跟着刷**，
  没用过就不白花一次请求


## [1.0.2] - 2026-08-18

### Changed

- **一次刷新只发一个请求，不再发四个。** 原来是：两次 `gh pr list`（作者 / 指派）+ 一趟
  GraphQL 拉状态 + 一趟拉 CI 明细。现在全挤进一条 GraphQL —— PR 状态、CI 明细、
  以及「作者是我」「指派给我」两个 `search`，一次拿回。

  为什么这么改：实测这条网络到 `api.github.com`，**TCP 连接只要 2ms，TLS 握手却要 1–5 秒**。
  而 `gh` 每调用一次就是新进程、新连接、新握手。所以耗时几乎只跟**请求个数**有关，
  跟数据量基本无关：

  | | 耗时 |
  | --- | --- |
  | 同一个查询，1 个 PR 别名 | 5.1 s |
  | 同一个查询，20 个 PR 别名 | 6.6 s |
  | 3 个请求，各自新建连接 | 14.6 s |
  | 3 个请求，复用同一条连接 | 7.2 s |

  所以「多带 190 KB 的 CI 明细」（+2 秒）比「多一次往返」（+5 秒）划算得多 ——
  上一版为了省流量拆成两趟，方向反了。

  实测同一时段头对头：**旧流程 11.7 秒 → 新流程 9.4 秒**；而旧流程那 11.7 秒还是最好情况
  （项目里没有 open PR，第二趟被跳过），有 open PR 时是 ~17 秒。

  代价：新导入的 PR 要等下一拍刷新才有状态（搜索和状态是同一次请求带回来的，
  先后顺序反了）。换一次往返不值。

### 说明

单次请求 ~9 秒里有 ~5 秒是 TLS 握手，这是这条网络的地板 —— TLS 会话恢复也是 5 秒
（实测完整握手 5.3–5.8 秒、会话恢复 5.0–5.3 秒），说明不是加解密开销，而是按 SNI 的定向延迟。
再往下只能靠**保持一条连接不断开**（实测热连接上后续请求只要 ~1.1 秒），那需要
应用自己维持 HTTP 连接而不是每次 fork 一个 `gh`。


## [1.0.1] - 2026-08-18

### Fixed

- **CI 明明没跑完，界面显示「CI ✓」。** 两个独立的原因，都修了。

  **一、分支保护要求的必过项，从来没报上来过。** GitHub 界面上那条
  「Some checks haven't completed yet — 8 expected」「Expected — Waiting for status to be reported」，
  指的是分支保护里配了必过、但这个 commit 上一次都没出现过的 check。它们不是 check run、
  不是 check suite、也不是 status context —— **所有 check 接口都看不见它们**，
  连 `gh pr checks` 都看不见（它只会说 19 pass / 23 skipping）。

  现在多拉一次 `repos/{repo}/branches/{分支}`，从 `protection.required_status_checks.contexts`
  拿到必过项清单，跟已报上来的比对，缺哪怕一项就是「运行中」。这个接口**只要读权限**就能读
  （实测 `admin=false`、`push=false` 也读得到），比要 admin 的 `/branches/{b}/protection` 宽松。
  实测某 PR：要求 14 项、报上来 34 个 check、缺 8 项 —— 跟界面上那个 8 一模一样。
  只对**还开着的** PR 做这个判断，已合并/关闭的追究这个没意义。

  **二、不再直接信 `statusCheckRollup.state`。** 那个字段算不准，而且**取决于你怎么问** ——
  同一个 commit、同一时刻，各查 4 次：

  ```
  statusCheckRollup { state }                     -> SUCCESS SUCCESS SUCCESS SUCCESS
  statusCheckRollup { state contexts(first:100) } -> FAILURE FAILURE FAILURE FAILURE
  ```

  因为它把**同名 check 的历次尝试**都算进去了：某项失败后重跑成功，旧那条 FAILURE
  还挂在 commit 上（实测某 PR 54 条 context，按名字去重只剩 34 项）。现在同名只取最新一次尝试。

  **只认 IN_PROGRESS 的 check suite，不认 QUEUED**：仓库上装的每个 GitHub App 都会给每个
  commit 挂一个 suite，多数一条 check run 都不发，永远停在 QUEUED（实测 StarRocks 每个 commit
  挂着 5 个这样的空 suite）。认 QUEUED 的话，早就合并的 PR 会永远显示「CI 运行中」。

### Changed

- **check 明细只给还开着的 PR 拉。** 刷新分两趟：第一趟只要基本字段（便宜），
  第二趟才给 open 的 PR 拉 check 明细和必过项清单。check 明细很贵 ——
  20 个 PR 能有 1300 多条 context、190 KB 响应，而已合并/已关闭的 PR 追究 CI 早就没意义了。
  实测一个 20 个 PR 全是已合并/关闭的项目，第二趟直接跳过，刷新只要 ~2 秒。

  副作用：**已合并 / 已关闭的行不再显示 CI 徽标**。那本来也是噪音 ——
  状态已经是终局，而且旧尝试留下的假红经常挂在上面。

  必过项清单按 `repo@分支` 缓存到进程退出，同一个分支只查一次。

## [1.0.0] - 2026-08-11

到这一版为止，一个新用户拿到 App 之后不用问人也能把它用起来 —— 这是 1.0.0 的门槛。

### Added

- **首次打开自动走一遍聚光灯导览**。四周压暗，把当前要讲的控件挖个洞露出来并描一圈边，
  旁边浮一个气泡说明它的作用，「下一步 / 上一步 / 跳过」逐步走完 8 步：
  项目标签、描述块、状态颜色、折叠区、编辑开关、拖动手柄、刷新、设置
- 设置的「关于」里可以「重新查看」导览
- **每行显示 PR 的目标分支**。一组 backport 标题一字不差，唯一的区别就是打到哪个分支上；
  不显出来，几行长得一模一样的 PR 看着就像重复了

### Changed

- **新导入的 backport 按编号倒序插到父块下面**，不再一律追加到末尾 —— 新的排在旧的前面，
  跟根级「新的落最上面」是同一个意思，免得刚开的 backport 沉到一堆已合并的下面。
  只给新来的挑位置，**不重排已有的兄弟**：同层顺序被手工调过的，插到第一个比它小的前面就停
- 根级仍是新的落在所有根节点最上面（含描述块之上），行为没变，补了断言钉住

### Fixed

- **多重 backport 挂错了父节点**。backport 的 backport，标题会一路带上来源
  （`…… (backport #77408) (backport #77463)`）。原来取第一个引用，于是挂到了最上游那个，
  跳过中间一环；现在取**最后**一个 —— 那才是直接的上一手，先后关系才是连着的。
  已经摆错的会在下次刷新时搬到正确位置，**一次性**的：跑过就置位，
  免得你手动拖到别处后下一拍又被搬回来。只动「当前父节点正好是标题里某个更早引用」的那些，
  你自己拖过的、已经挂对的都不碰
- **子层的块拖不动顺序**。投放缝原来只在根级之间有，子块之间没有 —— 同一个父块下
  想调两个 PR 的先后只能靠删了重加。现在每一行前面都有缝，缝跟着所在层缩进，
  哪一层都能调同级顺序

### 实现要点

- **每一步都指着一个真实控件**，没有纯说明页。挖不出洞的那一步看着就只是个居中对话框，
  那不叫导览 —— 测试里把这条钉住了
- **导览期间铺一份样板数据**（编号 900+，两个 backport 挂在主干 PR 下面）。
  首次打开时用户一条数据都没有，指着空看板讲「描述块」「状态颜色」等于指着空气；
  样板还刻意覆盖「可以合了 / 等上游 / CI 挂了」三种状态，颜色那一步才讲得清
- 洞是用 even-odd 填充规则在遮罩上真挖出来的，不是画个亮框 —— 只有真空洞才能让
  下面的内容原色透出来
- **锚点用 overlay 里的透明层上报**，不能直接把 `anchorPreference` 挂在控件自己身上：
  挂在父视图上会盖掉整棵子树的同键上报，描述块一上报，它内部折叠区的锚点就没了
- 讲拖拽那一步会把手柄单独显出来（那东西只在编辑态存在），但不动用户的编辑开关

## [0.9.0] - 2026-08-07

### Added

- **设置里可以检查更新并一键升级**。「更新」一节显示当前版本与检查结果，
  有新版本时给「下载并安装」按钮：下载 DMG → 校验签名 → 替换 `/Applications`
  里的 App → 自动重启
- **启动时自动检查**（默认开）。自动检查失败时安静收场，不拿网络问题烦人；
  手动点「检查更新」失败才会报错
- **「查到新版本就自动安装」开关，默认关**。替换正在运行的 App 会让它重启，
  这种事不该在你不知情时发生

### 实现要点

- 走 `gh` 而不是匿名 HTTP：GitHub 对未认证请求限 60 次/小时且按 IP 计，
  公司出口 IP 后面很容易撞上 403；`gh` 带认证，限额 5000 次/小时。
  顺带也不用再引一套网络栈，仓库将来转 private 也不用改
- **版本号按数字逐段比较**，不能用字符串 —— `"0.10.0" < "0.9.0"` 是字符串序，是错的
- 安装前校验下载包的签名，坏包直接中止；装上一个签名坏掉的 App 等于装了个打不开的东西
- 替换正在运行的自己没法在进程内完成，所以交给一个脱离出去的脚本：
  它等本进程退出后再替换并重新打开

## [0.8.1] - 2026-08-07

### Changed

- **状态配色重排**。原来「等 review」「等依赖」「草稿」都是灰色，而「已关闭」是粉色 ——
  灰色本该表示「已经没戏了」，用在「等 review」上等于说这个 PR 死了，而它其实是活的、
  可以去催的。新规则按「这件事欠在谁身上」分：
  绿=可以动了、红=欠在你身上、橙=等机器、蓝=等人、靛=等上游、紫=已合并、
  **灰只留给已终结的**（已关闭 / 草稿 / 未知）
- 徽标不再额外压暗「已完成」的项 —— 配色本身已经把它们压成灰或紫，再压一次会把
  已合并的紫色也抹掉
- **创建人挪到 PR 行右侧**，和上面一行的状态排成一列。原来在第二行最左边、
  灰色小字，很容易忽略。**不是你创建的会加重显示** —— 机器人代建的 backport
  和自己开的 PR 处理方式不一样，值得一眼分得出

## [0.8.0] - 2026-08-07

### Added

- **backport 自动聚合到父 PR 下面**。PR 标题里的 `(backport #77408)` 就是明确信号，
  认出来直接挂到父 PR 下 —— backport 本来就是「等主干那个先合」的先后关系，
  不该还要手工拖一遍。也认 `backport of #N` / `cherry-pick #N`。
  实测 51 个 PR 里 40 个能解析出父 PR，剩下 11 个正是主干 PR 本身
- **已关闭 / 已合并的 PR 也会导入**，但**只收和已跟踪 PR 有关系的** ——
  是某个已跟踪 PR 的 backport，或是某个已跟踪 backport 的父 PR。
  这样「主干合了但某个 backport 被关掉了」会直接显在树上，
  而不是把历史上几十个无关 PR 全倒进来

### Changed

- 挂进折叠的父 PR 时自动展开，别让新导入的东西藏起来

## [0.7.1] - 2026-08-07

### Changed

- **自动导入的范围从「作者是我」扩大到「作者是我 **或** 指派给我」**。
  机器人（如 `app/mergify`）自动创建的 backport PR 作者是 bot、只把你设成指派人，
  光看 author 会全部漏掉 —— 实测在 StarRocks 仓库里，只有 assignee 能查到的有 23 个
- 两个条件在 `gh pr list` 里是 AND 关系，所以分两次查再取并集；一条失败另一条仍可用，
  两条都失败才算这轮导入失败

## [0.7.0] - 2026-08-07

### Added

- **自动导入我的 open PR**。每次刷新会先查当前 gh 账号在该项目仓库下所有 open 的 PR，
  没见过的落到根级最上面，再拉状态。以前必须手工一个个填编号 —— 而 PR 编号本身
  还得先去 GitHub 上找，等于让人干一遍机器该干的活
- 设置里可以关掉这个行为（「刷新」一节）

### Changed

- `Project` 增加 `imported` 字段，记录每个导入过的 PR 编号。**每个 PR 只导入一次**：
  删掉之后不会被导回来，已合并 / 已关闭的也不会被自动清走 —— 树上游那些已合并的 PR
  正是判断下游是否解除阻塞的依据，不能让它们消失
- 树里已有但 `imported` 没记的（老数据、手工加的）会在首次刷新时补记，自动完成迁移

### Fixed

- **签名证书不再落在 `dist/`**。`make_signing_cert.sh` 默认输出改到
  `~/Library/Application Support/PrWaiter/signing/ci/`，与 MacBarWaiter / KeyboardWaiter
  一致。`dist/` 是构建输出目录、语义上随时可清空，长期密钥搁那儿早晚被误删
- **密码不再只打印**。原来 `make_signing_cert.sh` 把密码打到 stdout 就完了，
  终端一关那张 p12 就是打不开的废文件 —— 比丢证书更麻烦，因为你以为自己还有备份。
  现在会写进 `ci-signing.password`
- **重跑不再静默覆盖证书**。这张证书的价值全在「固定不变」，误跑第二次就换了身份、
  装过旧版的用户等于装了个不同的 App。现在默认拒绝覆盖，要换得显式 `FORCE=1`

## [0.6.1] - 2026-08-07

两个都是持久化相关的修复 —— 在讨论「要不要换 SQLite」时查出来的。

### Fixed

- **落盘改成原子写**。原来是 `data.write(to:)`，默认非原子 —— 先截断目标文件再写，
  崩在中间就只剩半个文件，整份数据都没了。加上 `options: .atomic` 后先写同目录的
  临时文件再 rename，rename 在文件系统层面是原子的
- **落盘失败不再被静默吞掉**。原来是 `try?`，磁盘满了或权限坏了界面上一点提示都没有，
  你以为改动存下了其实没有。现在会在列表顶部显示红色横幅、带系统给出的具体原因和
  「重试」按钮。这条排在拉取失败的提示之前 —— 后者只是数据旧了，前者是改动真丢了
- `release.sh` 收尾那句 `echo "已推送 $TAG，..."` 会报 unbound variable —— bash 把
  紧跟其后的中文逗号算进了变量名。tag 其实已经推成功，但脚本以非零码退出，
  看着像发版失败。改成 `${TAG}`

## [0.6.0] - 2026-08-07

第一个可下载的版本。

### Added

- **发布流水线**：打 `v*` tag 触发 GitHub Actions 构建 `.app`、签名、打成带
  Applications 拖拽目标的 DMG 并创建 Release
- **渐进式签名**：没配 secret 走 ad-hoc；配了 `MACOS_CERT_P12` 用固定自签身份；
  再配上 Apple 凭据则额外公证。三档都能出包，只是 Gatekeeper 的表现不同
- `scripts/make_signing_cert.sh` 生成固定的自签证书（不需要 Apple 开发者账号），
  打印出要贴进 GitHub Secrets 的值
- `scripts/bootstrap_local_signing.sh` 在本地建立稳定签名身份，自己构建时用
- `scripts/package_app.sh` / `scripts/make_dmg.sh` 本地也能出一样的产物
- `release_notes/v<版本>.md`：发布说明单独维护，CI 优先用它，没有才自动生成

### Changed

- `release.sh` 从「自己构建上传」改成「校验 + 打 tag 推送」，构建交给 CI。
  校验包括：工作区干净、在 main 上、与 origin/main 一致、CHANGELOG 有对应小节、测试通过
- `build.sh` 支持 `APP_DIR` / `APP_VERSION` / `BUILD_NUMBER` 环境变量，
  好让打包脚本复用它，不必把 swiftc 那行抄第二遍
- 去掉 `release.sh` 里「0.x 拒绝发布」的闸门

## [0.5.1] - 2026-08-07

### Added

- **项目标签也可以拖动排序**（编辑模式下）。和根级块一样，标签之间有投放缝，
  悬停时显示竖插入线。标签从 Button 换成普通视图 + 点击手势 —— Button 会吞掉拖拽手势

### Changed

- **切换项目标签不再重新拉取**。原来切标签会清空缓存再发请求，于是先闪一片「拉取不到」
  再等网络回来，体感很差。现在按项目分开缓存，切过去上次的数据立刻显示；要最新的
  自己点刷新，定时器下一拍也会带上。只有从没拉过的项目才拉一次，不然会是一片空白
- **「更新于」时间戳跟着项目走**。原来是全局一个，切过去会显示别的项目的拉取时间，
  是骗人的

### Fixed

- 删除项目时会一并清掉它的缓存，不再留下孤儿数据

## [0.5.0] - 2026-08-07

### Added

- **根级块可以拖动调整顺序**。根级块之间多了一条投放缝，平时看不见，拖拽悬停时显示成
  插入线。落在块**身上**是「成为它的子块」，落在块**之间**才是「排到这个位置」——
  两种意图靠落点区分。嵌套的块拖到缝里等于同时出组 + 定位
- **描述块右侧改成按状态分项的徽标**，代替原来的「N 个 PR · M 个可合并」
- **设置里新增「状态说明」一节**：列出每个图标对应哪个状态，以及什么情况下会落到那个状态。
  徽标上只有图标和数字，得有张对照表；但那是查一两次就记住的参照材料，
  不该长期占着主界面一行，所以放设置里。即时查询有徽标的悬停提示兜着

### Changed

- **新建的描述块 / PR 放在最上面**，不用滚到底才看得到刚加的东西
- **PR 状态从 6 种拆成 10 种**：原来的「等 review/CI」太笼统，现拆为
  `CI 失败` / `需修改` / `CI 运行中` / `等 review`，另外把 `草稿` 独立成状态。
  分类互斥，所以描述块上的分项数加起来正好等于 PR 总数
- 状态优先级把「该你动手的」排在「等上游」前面：CI 红了或被要求改，即使上游还没合
  也该先修，不该被「等依赖」盖掉
- 顶部原来的整体汇总（可合并的 PR 编号、各状态计数）去掉 —— 每个描述块已经有自己的分项，
  再来一份整体的是重复。「清理已合并」按钮挪进编辑态的操作栏，并改用 `allRows`，
  折叠起来的已合并 PR 现在也会一起清（原来会漏）
- PR 行上的「草稿」徽标去掉，状态标签已经涵盖

## [0.4.0] - 2026-08-07

### Added

- **App 图标**。图形是 PR 的树形分支，和界面里的缩进连线同一套视觉；最后一条用绿色，
  对应「可合并」这个状态。下方是 WLOBY 字标，PR 两字用绿色区分，与绿色分支呼应
- **图标由代码现画**（`make-icon.swift`，AppKit 绘制），构建时生成 `.icns`，
  仓库里不放二进制资产。改配色或版面改代码即可，改动在 diff 里看得见
- **小尺寸自动降级**：128px 以上画完整字标；64px 及以下只留图形；16px 进一步简化成
  两层分支。macOS 图标常以 16/32px 出现，七个字符横排在那个尺寸下只会糊成一团，
  而 `.icns` 允许每个尺寸用不同画面
- 细线和细条在小尺寸会退化到亚像素而消失，给了最小像素下限

## [0.3.2] - 2026-08-07

### Changed

- **窗口标题条彻底清空**，只留红黄绿三个按钮和「PrWaiter v0.3.2」。上一版把按钮塞进
  原生 toolbar，等于又把标题条弄脏了
- **刷新、编辑、设置三个按钮下移**，与项目标签同处一行：左边项目标签，右边操作按钮
- 设置页的返回按钮也移到这一行的左侧，不再挨着红黄绿
- **时间戳与刷新按钮分开摆**，不再挤在同一个胶囊里；时间戳区域宽度固定，
  刷新时按钮不会左右跳

## [0.3.1] - 2026-08-07

界面收拾了一轮，去掉自造的轮子，改用系统原生的那套。

### Changed

- **设置从弹窗改成界面内的切换页**。原来是盖在看板上的 sheet，现在看板和设置是同一个
  界面的两个页面，工具栏齿轮切过去，设置页左上角一个返回箭头切回来（Esc 也行）
- **删掉自造的应用内标题栏**，应用名和版本号交给窗口自己的标题条，按钮改用原生 toolbar
  直接长在同一条上 —— 少了一整行，内容多显示一行，也不再有两条标题栏并排的怪相
- 设置页的工具栏只留返回箭头，右侧清空 —— 刷新、编辑、齿轮都只对看板有意义
- **设置改用原生 `Form` + grouped 样式**：左标签右控件，分区带说明，随窗口宽度自适应。
  之前是自己拍了个 `maxWidth: 640` 的魔数，宽窗口下内容挤在左边、右边一大片空

### Fixed

- 设置里的自定义路径输入框在 grouped form 下没有边框、看不出可输入，且 `LabeledContent`
  会把 TextField 自带的 label 渲染到框外

## [0.3.0] - 2026-08-07

把 gh 这个前置依赖从「文档里的一句话」变成「App 里能处理的事」。

### Added

- **设置面板**：标题栏齿轮进入。gh 没配好时齿轮会变橙色带角标，主界面的错误横幅上
  也多了「去设置」直达按钮
- **一键安装 gh**：检测到 Homebrew 就直接跑 `brew install gh`，输出实时滚动显示
  （brew 可能要跑几分钟，不能只给个转圈）。没有 Homebrew 则给出 brew.sh 与
  gh 官方安装包的入口 —— 引导安装，而不是把 37MB 的二进制打包进 App
- **登录入口**：`gh auth login` 要选协议、按回车开浏览器，是交互式的，塞不进后台进程，
  所以按钮会开一个终端窗口把命令跑起来
- **自定义 gh 路径**：装在非常规位置时可以手工指定
- **可配置的刷新间隔**：30 秒 / 1 分钟 / 5 分钟 / 只手动刷新，原来固定 60 秒
- 设置里显示 gh 版本、登录账号、可执行文件路径与数据文件位置（可在 Finder 中显示）

### Changed

- gh 查找从写死三个路径改为：自定义路径 → Homebrew / MacPorts / 用户级目录 →
  `PATH` 里的每一段 → 最后用登录 shell 跑一次 `command -v gh`。
  GUI App 不继承 shell 的 PATH，装在非常规位置以前会直接找不到
- 找不到 gh 时的报错改为指向设置面板，不再让人自己去敲命令

## [0.2.1] - 2026-08-07

树的可读性改进。

### Added

- **折叠**：每个有子块的块，最左边隔出一条整高的折叠区，整条都可点，不用去瞄小三角。
  折叠状态会存进 `prs.json`（`collapsed` 字段），重开 App 依然保持。
  冻结模式下也能折叠 —— 它只是换个看法，不算改内容
- 折叠时块上显示 `▸ N` 徽标，提示里面藏了多少个块
- 拖东西到已折叠的块上会自动展开，避免看起来「拖没了」

### Changed

- **缩进连线**：同层的块之间画竖线，末位画 `└`、非末位画 `├`，层级关系一眼可见。
  祖先已是同级最后一个时，对应列留空而不是画到底
- 折叠只影响显示，不影响统计：顶部汇总与描述块的「N 个 PR · M 个可合并」
  仍然把折叠起来的 PR 算在内

## [0.2.0] - 2026-08-07

从「一个仓库的 PR 依赖列表」变成「多项目 + 描述块 + 可拖拽的树」。

### Added

- **项目标签页**：顶部一排标签，每个标签是一个仓库，点击切换后只看该项目的 PR
- **描述块**（topic）：可以写功能 / bug 说明的文字块，作为根级容器把相关 PR 收在下面；
  标签右侧显示该块下辖的 PR 数与其中可合并的数量
- **拖拽组织**：拖动任意块改变层级 —— PR 拖进描述块归类，PR 拖到 PR 下面表示先后关系，
  拖到底部虚线区回到根级；拖动会连子树一起搬
- **编辑 / 完成模式**：默认冻结，只读展示，避免误拖误删；点「编辑」才出现拖动手柄、
  增删按钮和输入框
- 纯逻辑测试（`test.sh`）覆盖树操作、拖拽合法性、数据迁移与 JSON 往返，并接入 CI

### Changed

- 数据格式改为 `{projects: [{name, repo, nodes: [树]}]}`，节点分 `topic` 与 `pr` 两种；
  首次启动会自动把 0.1.0 的 `{repo, prs}` 迁移过来，无需手工处理
- 先后关系由原来的 `after` 数组改为树的父子结构，一个 PR 只有一个前置。
  迁移时 `after` 的第一个依赖成为父节点，其余降级写进备注
- 新增或未归类的 PR 一律先落在根级，之后靠拖拽归类
- 删除一个块时，其子块提升一层而不是跟着删掉

### Fixed

- PR 编号不再被本地化成 `#77,255`（`Text` 的 `LocalizedStringKey` 会给整数加千位分隔符）

## [0.1.0] - 2026-08-07

首个可用版本。

### Added

- macOS 原生窗口 App（SwiftUI 单文件，`swiftc` 直接构建，无需 Xcode 工程）
- 记录 PR 之间的先后依赖关系，列表按依赖缩进成树状展示
- 通过 `gh` CLI 的单次 GraphQL 批量查询实时拉取 PR 标题、作者、review 结论、CI 结论与合并状态
- 四档派生状态：**可合并**（依赖全部合并 + 已批准 + CI 通过）、**等 review/CI**、**等依赖**、**已合并/已关闭**
- 顶部汇总栏直接点名当前可以催合并的 PR，并提供「清理已合并」一键移除
- 卡片内可增删依赖、编辑备注、删除记录，PR 编号可点击跳转 GitHub
- 每 60 秒自动刷新，也可手动刷新
